const powerCtrl = require('./power/power.ctrl')
const commandsCtrl = require('./commands/commands.ctrl')
const mouseCtrl = require("./commands/mouse.ctrl");

module.exports = {
  powerCtrl,
  commandsCtrl,
  mouseCtrl
}
